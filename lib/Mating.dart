import 'package:flutter/material.dart';

class MatingPage extends StatelessWidget {
  const MatingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Mating',style: TextStyle(fontSize: 60),
      ),),
    );
  }
}
